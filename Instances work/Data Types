Как получить определённые типы данных:

1) nil: просто использовать nil или же переменную которую ранее не обозначили
 Пример получения: print(a) -- nil; print(nil)

2) nan (not a number): получается при ошибке арифметического вычисления 
  Пример получения: print(1/0) -- nan; print(math.huge * math.huge) -- nan
  
3) void: получается когда Lua функция ничего не возвращает. Можно отобразить void только с помощью print и warn, а все остальные функции игнорируют void
  Пример получения: 
    local function test()
      return 
    end
    print(test()) -- 

4) null: получается когда C++ функция ничего не возвращает. null как и void можно отобразить только с помощью print и warn
  Пример получения: print(Instance.new("Part"):Destroy()) -- 
  
5) string: строка. Обычный тип данных. Для получения нужно между текстом поставить запятые. Библиотеку string можно использовать даже не получая саму библиотеку
  Пример получения: print("a", 'a', `a`, [[a]]) -- a a a a; print(("a"):upper()) -- A
  
6) number: число. Обычный тип данных
  Пример получения: print(1) -- 1; print(2 + 2 * 2) -- 6
  
7) boolean: истинна или ложь. Получается при сравнении
  Пример получения:
    local function test(val)
      if val >= 6 then
        return true 
      end
      return false
    end
    print(test(2 * 2)) -- true
    
8) table: тип данных которые хранят другие данные. Есть два типа: array и dictionary и каждые из них нужно использовать по-разному
  Пример получения:
    print({}) -- table: AddressCode; print(unpack({1, 3, 5})) -- 1 3 5
    
9) function: сложный тип данных который может что-то делать или возвращать. Это единственный тип данных которые нельзя использовать в LuaU подсказках и нельзя получить содержимое обычным способом
  Пример получения:
    local a = function()
      return 1 + 3
    end
    print(a) -- function: AddressCode
    
10) thread: получается когда переменная может создать новый поток скрипта или задерживать текущий поток
  Пример получения:
    local a = task.spawn(function()
      return 1
    end)
    print(a)

11) userdata: получается для представления кода С++ в Lua/LuaU
  Пример получения:
    print(type(workspace.Part)) -- userdata; print(type(newproxy(true))) -- userdata
    
                           Специальные типы данных
Эти типы данных не являются основными и в основном используются для типизации в LuaU:

1) any: значение может быть любым
  Пример:
    local x: any = funct()
    x.foo() -- компилятор не будет конфликтовать 
    
2) unknown: значение может быть любым, но с ним нельзя ничего делать до обозначения 
  Пример:
    local x: unknown = funct()
    -- x.foo() -- выведет ошибку
    local s = x :: string
    
3) never: означает, что функция никогда не возвращает что либо 
   Пример: 
     local function err(text: string): never
       error(text)
     end
     
4) union: тип который может быть одним из нескольких вариантов
  Пример:
    local v: string | number = math.random() > 0.5 and "hi" or 5

5) optional: применяется к переменным где значение может отсутствовать
   Пример:
     local name: string? = nil
